setup:
  config:
      LIVE_DB_HOST: us-cdbr-east-03.cleardb.com
      DB_USER: bbe0981cb28eb1
      LIVE_DB_PASSWORD: be3b9210
      DB_NAME: heroku_4adccb1d179d193
      DEPLOY: live
      EVENT_REGION: tw
      DOMAIN: shortenurl.work.tw
      LANGUAGE_CODE: zh-hant
      TIME_ZONE: Asia/Taipei

build:
  docker:
    web: Dockerfile.openresy
    app: Dockerfile.gunicorn
  config:
      REQUIREMENTS=server

run:
  web: sh -c 'sed -i \"s/172.16.0.10/127.0.0.11/g\" /usr/local/openresty/nginx/conf/nginx.conf && echo \"\" > /etc/hosts && openresty -p .'
